{
  "name": "puppetbots-hands",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/bundle.js",
    "watch": "esbuild src/index.ts --watch --bundle --minify --sourcemap --outfile=dist/bundle.js",
    "dev": "concurrently \"npm run watch\"  \"npx http-server\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "esbuild": "^0.14.11",
    "http-server": "^14.1.0"
  }
}
